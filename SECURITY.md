# 安全

## 对外口径（仅此三句，不扩大）

| 对象 | 承诺 | 机制 |
|------|------|------|
| 同步节点运营方（含你自己） | 零知识 | E2EE；节点只见密文、grant、脱敏类别标签、尺寸、时间戳 |
| Agent 运行时 | 只见授权范围 | 每 Agent 独立密钥 + ECIES grant；撤销即删 grant |
| 模型厂商 | 最小化暴露，**不承诺保密** | 按需注入片段；推理需要明文 |

禁止任何「对模型厂商保密」的表述。

## 已知限制

1. **本地已缓存明文无法远程擦除。** 撤销 Agent 后，该 Agent 磁盘上已有的明文副本不会消失。UI 与文档必须如实说明。
2. **SPAKE2 为自编排实现。** 使用 `@noble/curves` / `@noble/hashes` 原语，经 RFC 9382 官方测试向量与恶意中继测试。**未做第三方协议审计。** 生产敏感场景优先用助记词在新设备恢复 MK，把 SPAKE2 配对当作便利增强。
3. **认证是设备 token。** 不是公钥签名挑战。保护好 `device_token`（与配置文件权限）。
4. **平台 Keychain 不可用时降级为 0600 文件。** setup / 管理台会标明；不要把文件 Keychain 当成生产默认而不自知。
5. **无遥测。** `telemetry.opt_in` 默认 false；即使将来开启也只允许行为元数据，禁止回传记忆内容。

## 漏洞披露

请不要公开提交含利用细节的 Issue。将报告发到仓库维护者（通过 GitHub Security Advisory，或维护者公布的邮箱）。我们会确认、修复、再公开。

当前项目未声称已通过渗透测试。把本仓库当可审计源码，而不是已背书的安全认证。
